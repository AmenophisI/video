# Samsung実機画面 Design QA

## 比較条件

- 実機: Samsung SC-03L / Android 12 / One UI 4.1
- 物理解像度: 1440×3040
- オーバーライ密度: 560 dpi
- 比較元: 匿名化済みの `PL-01` / `PL-02` / `PL-03` / `PL-04` / `PL-11`
- 検証素材: Samsung電卓画面だけを収録した安全な一時合成動画

## 比較エビデンス

- `qa-evidence/PL-01_comparison.png`: 縦画面の再生コントロール
- `qa-evidence/PL-02_comparison.png`: プレイヤーのその他メニュー
- `qa-evidence/PL-02_live-device_comparison.png`: 左がSamsung純正7.3.29.1、右が最終実装。同一の安全な合成動画・同一端末で比較
- `qa-evidence/PL-03_comparison.png`: 動画詳細（8行）
- `qa-evidence/PL-04_comparison.png`: プレイヤー設定（単一カード・7行）
- `qa-evidence/PL-11_comparison.png`: 横画面の再生コントロール

各画像は左に参照画像、右に最終実装の実機キャプチャを配置した。実装側の映像面は安全な合成動画で、私的な動画・ファイル名・保存場所を含まない。

## 修正反映

1. 再生画面をSamsung実機の操作階層へ再構成し、中央大型再生、10秒戻し／進み、ミュート、プレイリスト位置、上部戻るボタンを通常表示から除去した。
2. 実機検証で、ネイティブ映像面が詳細・設定ルートより前面に残るP0差分を検出した。内部画面への遷移時に映像面を一時停止・非表示化し、復帰時に再生成するよう修正した。
3. 横画面のパネル開始位置、操作間隔、詳細画面の長いラベルを実機画像へ合わせて調整した。
4. 動画詳細を閲覧専用8行、設定を単一カード7行へ置換した。
5. 動画／音声コーデックと音声チャンネル数をAndroid取得、DTO、モデル、キャッシュDBへ追加し、取得不能値を `--` とした。
6. PL-02の追加確認で、再生コントロール面の青成分と映像色の透過が強い差分を修正した。オーバーレイを高不透明度のニュートラルチャコール、ポップアップを濃いニュートラルグレーへ変更し、SC-03Lで再撮影・再比較した。
7. その後、Samsung純正プレイヤー7.3.29.1を同じ合成動画で直接再調査した。匿名化済みPL-02から読み取っていた全面パネルは純正UIに存在しなかったため、再生コントロールの背景・枠線を完全に除去した。メニューを実測168dp幅、28dp角丸、右余白約10dpへ合わせ、行間・内側余白・点線区切り・無音動画での項目差分も純正状態へ修正した。
8. Samsung純正のジェスチャー操作に合わせ、映像面の左側を上下スワイプすると画面輝度、右側を上下スワイプするとメディア音量が変わるようにした。調整中は操作側へアイコン・縦ゲージ・割合を表示し、操作終了後に自動で消える。既存の横スワイプシーク、タップ、自動非表示との方向判定も統合した。

## 検証結果

- Flutter Widget / Unit Test: 59件 passed
- `flutter analyze`: passed（No issues found）
- debug APK build: passed
- 最終debug APKのUSB実機インストール: passed
- 縦、横、小画面、大文字、字幕有無、前後動画有無、ロック状態: レイアウト例外なし
- 動画詳細から編集・共有・削除操作が除去されていること: passed
- プレイヤー設定が7行、OFF／OFF／OFF／ONで表示されること: passed
- Androidメタデータ→DTO→Video→DBの保存・復元と非破壊マイグレーション: passed
- 内部の詳細・設定遷移がネイティブ映像面に隠れないこと: 実機確認 passed
- P0〜P2の未解消差分: なし
- Samsung純正7.3.29.1と最終実装のメニュー同一条件比較: passed
- 左側の上スワイプによる画面輝度上昇、右側の上スワイプによるメディア音量上昇、下スワイプによる低下: USB実機確認 passed

注: 実装キャプチャ外周に見える細い緑線は実機側のフォーカス表示であり、アプリのUI要素ではないため比較対象外とした。

## プライバシーと後処理

- 私的な動画やファイル名は比較エビデンスへ含めていない。
- 未加工の実機キャプチャは成果物へ残さない。
- 検証用の一時合成動画は実機から削除済み。

final result: passed
